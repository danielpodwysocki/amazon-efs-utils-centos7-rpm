name: Build the RPM
on: [push]
jobs:
  build-rpm:
    runs-on: ubuntu-latest
    container: rpmbuild/centos7
    steps:
      - run: git clone https://github.com/aws/efs-utils
        working_directory: /tmp
      - run: make rpm
        working_directory: /tmp/efs-utils

